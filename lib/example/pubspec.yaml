name: example
description: Demonstrates how to use the yourpackagename package. # Substitua yourpackagename pelo nome real do seu package
publish_to: 'none' # Impede a publicação deste exemplo no pub.dev
version: 1.0.0+1
environment:
  sdk: '>=3.0.0 <4.0.0' # Use a versão do SDK compatível com seu package
dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  nav_manager:
    path: ../..
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
flutter:
  uses-material-design: true